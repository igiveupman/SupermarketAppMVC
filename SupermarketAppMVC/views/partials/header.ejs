<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' | Supermarket App' : 'Supermarket App' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/css/styles.css" />
  <script defer src="/js/lighting.js"></script>
</head>
<body class="theme-fairprice"><!-- Custom FairPrice-inspired navigation replaces Bootstrap navbar -->
<% /* Main site navigation (FairPrice-inspired) */ %>
<nav class="main-nav shadow-sm">
  <div class="nav-inner container-wide">
    <a href="/" class="nav-brand">
      <img src="/images/logo.png" alt="UnfairPrice logo" class="nav-logo" />
      <span class="brand-text">UnfairPrice</span>
    </a>
    <button class="nav-toggle" aria-label="Toggle navigation">☰</button>
  <% /* Primary nav links differ for admin vs user */ %>
  <ul class="nav-links">
      <% if (user) { %>
        <% if (user.role === 'admin') { %>
          <li><a class="nav-link" href="/inventory"><span class="nav-icon">📦</span>Inventory</a></li>
          <li><a class="nav-link" href="/addProduct"><span class="nav-icon">➕</span>Add Product</a></li>
          <li><a class="nav-link" href="/admin/users"><span class="nav-icon">👥</span>Manage Users</a></li>
        <% } else { %>
          <li><a class="nav-link" href="/shopping"><span class="nav-icon">🛍️</span>Shop</a></li>
          <li><a class="nav-link" href="/cart"><span class="nav-icon">🛒</span>Cart <span class="cart-badge" data-cart-count><%= typeof cartCount !== 'undefined' ? cartCount : ((typeof cart !== 'undefined' && cart && cart.length) ? cart.reduce((s,i)=> s + i.quantity,0) : 0) %></span></a></li>
          <li><a class="nav-link" href="/favorites"><span class="nav-icon">❤️</span>Favourites</a></li>
          <li><a class="nav-link" href="/orders"><span class="nav-icon">📜</span>Orders</a></li>
        <% } %>
      <% } %>
    </ul>
  <% /* Right-side actions: login/register or user greeting + logout */ %>
  <div class="nav-actions">
      <% if (!user) { %>
        <a href="/login" class="nav-cta">Login</a>
        <a href="/register" class="nav-cta alt">Register</a>
      <% } else { %>
        <span class="nav-user">Hi, <strong><%= user.username %></strong></span>
        <a href="/logout" class="nav-cta">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
<main class="container-wide mt-4">
  <% if (messages && messages.length) { %>
    <div class="alert alert-success"><%= messages.join(', ') %></div>
  <% } %>
  <% if (errors && errors.length) { %>
    <div class="alert alert-danger"><%= errors.join(', ') %></div>
  <% } %>
