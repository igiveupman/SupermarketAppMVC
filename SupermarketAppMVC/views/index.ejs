<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Supermarket App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <% if (!user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/Login">Login</a>
                </li> 
            <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/shopping">Shop</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart">View Cart</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/Logout">Logout</a>
          </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <style>
      .hero {
        min-height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
      }
      .hero-card { max-width: 820px; }
      .lead-cta { font-size: 1.1rem; color: #6b7280; }
    </style>

    <section class="hero">
      <div class="container hero-card">
        <div class="card shadow-sm">
          <div class="card-body text-center py-5">
            <h1 class="display-5 fw-bold">Supermarket App</h1>
            <p class="lead lead-cta">The only supermarket app for you.</p>

            <% if (user) { %>
              <p class="mt-4">Welcome back, <strong><%= user.username %></strong> <small class="text-muted">(<%= user.role %>)</small></p>
              <% if (user.role === 'admin') { %>
                <a class="btn btn-primary btn-lg me-2" href="/inventory">Go to Inventory</a>
                <a class="btn btn-outline-secondary btn-lg" href="/addProduct">Add Product</a>
              <% } else { %>
                <a class="btn btn-primary btn-lg me-2" href="/shopping">Start Shopping</a>
                <a class="btn btn-outline-secondary btn-lg" href="/cart">View Cart</a>
              <% } %>
            <% } else { %>
              <p class="mt-4">Create an account or sign in to start shopping.</p>
              <a class="btn btn-primary btn-lg me-2" href="/register">Register</a>
              <a class="btn btn-outline-secondary btn-lg" href="/login">Login</a>
            <% } %>
          </div>
        </div>
      </div>
    </section>
</body>
</html>